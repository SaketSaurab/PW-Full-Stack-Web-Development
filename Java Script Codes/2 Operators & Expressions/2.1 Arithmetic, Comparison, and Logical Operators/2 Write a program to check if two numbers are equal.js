// Write a program to check if two numbers are equal

function isNumEqula(a,b){
    if(a==b){
        console.log(`the numbers ${a} and ${b} are equal`);
    }
    else {
        console.log(`the numbers ${a} and ${b} are not equla`);
    }
}

let x1=15;
let y1=22;

let x2=22;
let y2=22;

isNumEqula(x1,y1);
isNumEqula(x2,y2);
