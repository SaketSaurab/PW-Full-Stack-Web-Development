//  Find the absolute difference between two numbers

function absoluteDifference(a,b){
    console.log(`the absolute difference between ${a} and ${b} is ${Math.abs(a-b)}`)
}

let a=10;
let b=20;
let c=-10;
let d=34;
absoluteDifference(a,b);
absoluteDifference(c,d);