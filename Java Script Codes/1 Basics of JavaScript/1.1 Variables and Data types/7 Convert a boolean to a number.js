// Convert a boolean to a number

let a=true;
let b=false;
console.log(`the value of a and b before conversion \n a=${a} and b=${b}`);

let x=Number(a);
let y=Number(b);
console.log(`the value of a and b after conversion \n a=${x} and b=${y}
    `);
