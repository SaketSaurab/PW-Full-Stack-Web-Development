// Check if a variable is undefined or null

let a;  // the value is assigned so it is undefined 
let x=null;  

console.log(`the value of a=${a} and type of a=${typeof(a)}\n vlaue of x=${x} and type of x = ${typeof(x)}`);