// Declare a variable and assign a number to it.

let a=2; // declaring a variable using let, we can later reassign another value in this 
const b=5;  // declaring a variable using const , but we can not change the value later.
var c=15; // declaring a variable using var, it was old method used to declare the variable but now this is not used 

console.log(`variable declared using let a=${a} \n variable declare using const b=${b} \n variable declared using var c=${c}`);
