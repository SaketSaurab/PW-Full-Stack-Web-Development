// Convert a string to a number

let a="123";
console.log(`the value of a${a} and the type of a is ${typeof(a)}`);

let b=String(a);
console.log(`the value of b=${b} and the type of b is ${typeof(b)}`);